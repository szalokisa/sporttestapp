FROM node:16-alpine
WORKDIR /app
COPY package.json .
RUN yarn
COPY . .

ENV DB_SERVER=<>
ENV DB_PORT=<>
ENV DB_DATABASE=<>
ENV DB_USER=<>
ENV DB_PASSWORD=<>
ENV DB_ENCRYPT=<false/true>
ENV DB_TIMEOUT=120000
ENV PORT=4000
ENV LOCAL_SYSTEM_IP=<>
ENV jwtSecretKey=<>

EXPOSE 4000

CMD ["yarn", "start"]
